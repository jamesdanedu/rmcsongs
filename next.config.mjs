/** @type {import('next').NextConfig} */
const nextConfig = {
  // Temporarily disable PWA to get the app working
  // You can add PWA back later once the base app is stable
};

export default nextConfig;
